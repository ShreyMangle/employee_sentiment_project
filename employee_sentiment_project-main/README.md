# Employee Sentiment Analysis Package

This package was auto-generated to match your uploaded dataset. Files included:
- data/data_used.csv (the dataset copy)
- employee_sentiment_analysis.ipynb (Jupyter notebook)
- employee_sentiment_project.py (runnable script)
- README.md (this file)
- .env.example

## How to run
1. Download and unzip the package.
2. Open the notebook `employee_sentiment_analysis.ipynb` in Jupyter or VS Code and run the cells.
3. Or run the script: `python employee_sentiment_project.py` (it expects `data/data_used.csv` in same folder)

## Column mapping detected

{
  "date": "date",
  "feedback": "Subject",
  "employee_id": "Subject",
  "employee_name": "Subject",
  "department": None
}

If the mapping is incorrect, open the notebook and update the `mapping` variable to point to the right columns.
